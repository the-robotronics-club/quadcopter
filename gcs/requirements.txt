# NIDAR GCS — dependencies
#
# Install with:
#   pip install -r requirements.txt

PySide6>=6.5        # Qt bindings — main window, widgets, QPainter map canvas, QWebSocket bridge client
websockets>=11.0     # only needed to run test_bridge_server.py (the mock PX4 companion bridge)
